# 2026-09-22

## Squadrone: la scheda del pilota (commit `2d9f21e`)

L'albero dei campi era incomprensibile ("Risorsa dell'equipaggio, Procedural
Texture, Samplers, Alt Id"). Ora c'e' `PannelloWingman`, un form con le sei cose
del vecchio editor (`nomanssave.ec`, scheda "Wingman"): **Attivo**, **Razza NPC**,
**Seed NPC**, **Tipo di astronave**, **Seed astronave**, **Grado pilota**.

**Le tabelle, prese dal vecchio editor** (`Piloti.java`):

- razze (`nomanssave.gy`): Korvax `NPCKORVAX.SCENE.MBIN`, Gek `NPCGEK...`,
  Vy'keen `NPCVYKEEN...`, Fourth Race `NPCFOURTH...`;
- navi (`nomanssave.gL`, 13 tipi): Fighter, Shuttle, Hauler, Explorer,
  **Exotic** (`S-CLASS/S-CLASS_PROC.SCENE.MBIN`), Living (`BIOPARTS/BIOSHIP`),
  Solar (`SAILSHIP`), Robot (`SENTINELSHIP`), Corvette (`BIGGS`),
  Utopia Speeder (`VRSPEEDER`), Golden Vector (`FIGHTERCLASSICGOLD`),
  Horizon Vector NX (`FIGHTERSPECIALSWITCH`), Starborn Runner (`WRACER`).

**Struttura da ricordare:** il seme e' il **secondo** elemento di una lista,
`Seed: [flag, "0x..."]`: si tocca solo la stringa. "Attivo" e' un elemento di
`SquadronUnlockedPilotSlots` (lista di booleani).

**Non esistono icone delle navi per tipo:** tutte usano `UI-SHIPICON.PNG`, il
tipo lo dice il nome scritto accanto.

Collaudo (`CollaudoWingman`): Korvax→Gek, Exotic→Fighter, seme nuovo, grado
10→25, riletti dal file, giro completo integro.

## Squadrone: icone della razza e della navicella (commit `d92f6ed`)

Le voci degli elenchi avevano per icona l'iniziale del nome. Ora:

- **Pilota dello squadrone**: la razza si legge dal modello nel salvataggio
  (`NPCResource.Filename` = `.../NPCKORVAX.SCENE.MBIN`) e l'icona e' una di
  quelle del catalogo (`UI-KORVAX`, `UI-GEK`, `UI-VYKEEN`). La **navicella** ha
  l'icona `UI-SHIPICON` e la **classe** (S/A/B/C) letta da `ShipResource.Filename`
  (`MODELS/COMMON/SPACECRAFT/S-CLASS/...`). Il sottotitolo dice
  "Korvax · Classe S · Grado del pilota 10".
- **Fregate**: la razza e' una gilda — `Warriors`, `Traders`, `Explorers` — e le
  icone sono `UI-WARRIORS`, `UI-TRADERS`, `UI-EXPLORERS`.
- **Compagni**: icona fissa `UI-PET.PNG`. Il sottotitolo leggeva `Type`, che non
  esiste: ora legge `CreatureType`.
- Sopra il form c'e' una **testata** con l'icona grande dell'elemento scelto.

**Non esistono icone delle navi nei pak del gioco** (verificato con l'elenco dei
file di HGPAKtool): la nave si vede in 3D, e l'unica icona navale e' quella
dell'interfaccia. Per questo la navicella ha l'icona generica piu' la classe.

## Basi e contenitori: due gruppi (commit `5d08585`)

Sedici schede in fila non si leggevano. Ora: gruppo **Contenitori** (Deposito
1..10, Deposito speciale, Deposito del razzo) e gruppo **Cucina e pesca**
(Ingredienti di cucina, Unita' di cibo, Esca da pesca, Piattaforma da pesca).
Misurato: 12 schede nel primo gruppo, 4 nel secondo.

**Convenzione nata oggi:** quando i depositi di una sezione sono tanti, le voci
prendono un `gruppo` e la sezione mostra prima i gruppi e poi i loro depositi.
Vale per Navi, Veicoli, Mercantile, Basi e contenitori.

## L'editor deve valere per qualunque salvataggio (commit `6234b7e`)

Sbri: "questo non e' un save editor personale, poi lo pubblico su github, percio'
deve essere in grado di riconoscere ogni salvataggio."

- **Veicoli**: la sezione c'era ma non mostrava niente. Ora e' una sezione a
  inventario come le navi: inventario + tecnologie per veicolo. Il salvataggio
  non dice che veicolo e' (nome e `Resource.Filename` vuoti): il tipo si ricava
  **dall'indice**, come faceva il vecchio editor in `nomanssave.gO.getType()` —
  0 Roamer, 1 Nomad, 2 Colossus, 3 Pilgrim, 5 Nautilon, 6 Minotaur, 1000 Skiff,
  altrimenti "Veicolo N". I veicoli **non** hanno `BaseStatValues`: nessun
  blocco statistiche.
- **Contesto attivo**: la radice ha `ActiveContext` ("Main" in questa partita) e
  il file tiene due copie dello stato (`BaseContext` e `ExpeditionContext`).
  `Inventari.risolvi()` ora sostituisce il prefisso `BaseContext.` con il
  contesto attivo: un salvataggio di spedizione non mostra piu' la partita.
- **Geometria della griglia**: se mancano `Width` e `Height`, si ricavano dalla
  casella piu' lontana fra sbloccate e occupate (prima si ripiegava su 1x1).
- Aggiunto `Finestra.apriScheda(gruppo, deposito)` per fotografare viste che di
  solito si raggiungono cliccando: serve allo strumento delle schermate.

## Correzione: i depositi sono due, non tre (commit `2556b65`)

Sbri: "la tuta non ha una stiva, le navi non hanno stiva e cargo, hanno
tecnologia e inventario come tutte le altre. Nessuno ha un doppio deposito.
Devi sempre verificare con il gioco."

Avevo supposto tre depositi per nave perche' il campo `Inventory_Cargo` esiste
nel salvataggio. Verificato: **esiste ma e' vuoto** — zero caselle sbloccate,
zero oggetti — per la tuta, per ogni nave e per il mercantile. Il criterio ora
e' `Inventari.esiste()`: `ValidSlotIndices` non vuoto oppure `Slots` non vuoto.
Risultato misurato: tuta 2 schede, ogni nave 2 schede, mercantile 12 schede su
due gruppi. Spariscono anche i multi-tool segnaposto.

Il mercantile ha in piu' i dieci contenitori di stoccaggio (`Chest1..10Inventory`,
50 caselle sbloccate ciascuno): sono un gruppo "Depositi" accanto a "Mercantile",
non tredici schede in fila.

Aggiunta la vista Mercantile allo strumento delle schermate.

## Navi: stiva, tecnologie e cargo. Griglia fedele al gioco. Statistiche della tuta

Commit `2bbe83f` (3 file: `GrigliaSlot.java`, `Inventari.java`, `PannelloInventario.java`).

**Il difetto vero, misurato sul salvataggio `EEC95D8A4074480B94EC48FD53A6C503`.**
Nel salvataggio `Slots` contiene solo gli slot **occupati**, ognuno con `Index{X,Y}`.
La stiva di "CORRIDORE TRA LE STELLE" ha **7 oggetti dentro 118 caselle attive**
(`Width=10`, `Height=12`, `ValidSlotIndices=118`): la griglia ne disegnava 7, e la
nave sembrava vuota. Stessa cosa per la tuta: 100 oggetti su 120 caselle, e il
contatore diceva "100 / 100" perche' prendeva il totale da `Slots.size()`.

Cosa e' cambiato:

- `GrigliaSlot` disegna `Width x Height` celle, sblocca quelle in
  `ValidSlotIndices` e mette ogni oggetto alla sua posizione `Index`.
  Celle libere = spazio reale; celle non sbloccate = spente.
- Celle libere riempibili (`imposta`) e svuotabili (`rimuovi`): nel salvataggio
  gli slot vuoti non esistono, quindi si crea/toglie la voce da `Slots`.
- `SpecialSlots` riscritto nella forma vera `{Type:{InventorySpecialSlotType:
  "TechBonus"}, Index:{X,Y}}`: prima ci si scrivevano numeri sciolti.
- `Inventari.navi()` genera tre inventari per nave (Stiva, Tecnologie, Cargo)
  con la nave come *gruppo*: schede su due file. Saltate le navi segnaposto
  (`Width*Height <= 1`, senza nome).
- Tuta: blocco "STATISTICHE PRINCIPALI" con Salute, Scudo, Energia, Unita',
  Naniti, Quicksilver — gli stessi campi del vecchio editor (`nomanssave.aJ`,
  estratto con `javap`: "Statistiche principali" + Health/Shield/Energy/Units/
  Nanites/Quicksilver). Percorsi: `PlayerStateData.Health/Shield/Energy/Units/
  Nanites/Specials`.
- Valute (Unita', Naniti, Quicksilver) lette e scritte **senza segno**: nel file
  le unita' sono `-1598048959`, nel gioco 2696918337.
- Contatore slot spostato sulla seconda riga: sulla prima le 9 schede delle navi
  lo schiacciavano a zero (era invisibile anche prima).

**Due trappole trovate nei dati, da ricordare:**
1. `ValidSlotIndices` scrive la posizione **sciolta** `{X,Y}`; gli slot la
   scrivono dentro `Index`. Leggere solo la seconda forma lasciava l'elenco
   degli slot sbloccati vuoto (tutte le caselle attive: 120 invece di 118).
2. `PlayerStateData.Specials` (mappato a "Quicksilver" in `Etichette`) vale
   2000000602: sembra una maschera, non un contatore. Da verificare.

## Collaudo

- `it.atlante.strumenti.Schermate` genera le schermate vere aprendo il
  salvataggio (1500x940). Verificate: Navi, Tuta, Multitool, Conquista stazioni.
  Le versioni in `schermate/` sono state rigenerate (commit `d36781f`).
- `it.atlante.Main giro <payload>` → "il giro completo conserva i dati", byte
  identici.
- Collaudo dei conteggi (in `%TEMP%\atl\CollaudoGriglia.java`): per ogni
  inventario stampa celle / attivi / occupati. Utile e riproducibile.
- Collaudo di scrittura (in `%TEMP%\atl\CollaudoScrittura.java`): copia il
  salvataggio, riempie una casella libera, svuota una occupata, salva e rilegge.
  **Da rifare ogni volta che si tocca la scrittura.**

## BUG BLOCCANTE trovato dal collaudo di scrittura (corretto, commit `1be420a`)

`Salvataggio.salva()` usava `File.renameTo` per sostituire il file. Su Windows
`renameTo` **non sovrascrive** un file che esiste gia': premendo "Salva" l'editor
si fermava con "impossibile sostituire il file" e non scriveva niente. Sostituito
con `Files.move(..., REPLACE_EXISTING, ATOMIC_MOVE)` e ripiego su
`REPLACE_EXISTING` se il file system non sa farlo atomico.

Questo significa che il salvataggio normale non ha mai funzionato su questa
macchina: da verificare se il vecchio editor aveva lo stesso problema (no, era
un altro codice) e se l'utente ha mai salvato davvero con Atlante.

